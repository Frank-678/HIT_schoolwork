#include <iostream>
#include <vector>
using namespace std;

int main() {
    int L, N, M;
    cin >> L >> N >> M;

    vector<int> D;
    int in;
    for (int i = 0; i < N; i++) {
        cin >> in;
        D.push_back(in);
    }

    // TODO


    return 0;
}