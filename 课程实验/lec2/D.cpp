#include <iostream>
#include <vector>
#include <algorithm>
#include <iomanip>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    int k, s;
    vector<pair<int, int> > candidates;
    for (int i = 0; i < n; i++) {
        candidates.push_back(make_pair(k, s));
    }
}